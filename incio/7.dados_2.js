let preco = 19.90;
let desconto = 0.4;

console.log(preco * (1 - desconto)); // Da para jogar em uma variavel tambem

let precoComDesconto = preco * (1 - desconto);

console.log("\nValor após o desconto = " + precoComDesconto);